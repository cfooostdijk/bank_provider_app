<div class="container mx-auto mt-5">
    <h1 class="text-3xl font-bold mb-4"><%= t("banks.shared.back_to_banks") %></h1>

    <%= render 'form' %>

    <a href="<%= banks_path %>" class="btn btn-primary text-white">
        <%= t('banks.shared.back_to_banks') %>
    </a>
</div>
