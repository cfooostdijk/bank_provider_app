<div class="container mx-auto">
	<h1 class="text-3xl font-bold mb-4"><%= t('banks.show.title') %>: </h1>

  <h2><%= @bank.name %></h2>
	<p class="text-gray-600"><%= t('banks.shared.bank_id') %>: <%= @bank.id %></p>

	<%= link_to t('banks.shared.edit'), edit_bank_path(@bank), class: "text-yellow-500 hover:underline mr-2" %>
	<%= link_to t('banks.shared.back_to_banks'), banks_path, class: "text-blue-500 hover:underline" %>
</div>
